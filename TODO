Redo README example
Fix crawlers
Update and verify CI



create X nodes
	generate Y resources per node

iterate over node directories
	count number of resources per node
	load node state, count neighbors per node

	get number of non-native resources per interval
	get number of non-bootstrap neighbors per interval
		will show how often peers/resources are being shared

	plot dataa


/analytics
    metrics.py
        gather
        plot & save